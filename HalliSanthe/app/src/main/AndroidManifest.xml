<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HalliSanthe">

        <!-- Splash / Role Selection -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.HalliSanthe.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Buyer -->
        <activity
            android:name=".ui.buyer.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Artisan -->
        <activity
            android:name=".ui.artisan.ArtisanDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.artisan.AddProductActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Detail -->
        <activity
            android:name=".ui.detail.ProductDetailActivity"
            android:exported="false" />

    </application>

</manifest>
